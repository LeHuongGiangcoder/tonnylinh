# The Google Apps Script web app (/exec URL) behind the guest list and RSVPs.
# See apps-script/Code.gs for how to deploy it.
RSVP_ENDPOINT=

# The same long random string as the script's SECRET property. Server-only:
# no NEXT_PUBLIC_ prefix, so it never reaches a browser. Never commit it.
RSVP_SHARED_SECRET=
